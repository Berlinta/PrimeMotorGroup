<div class="col-md-6 col-md-offset-3">

<h1>All Vehicles</h1>
    
    <%= params[:make] %>
      <% @cars.each do |car| %>
        <h4><%= car.name %> <%= link_to car.make, car_path(car) %></h4>
     <% end %>