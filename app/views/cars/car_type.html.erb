<div class="row">
  <div class="col-md-4">
  </div>
  <div class="col-md-8">
    <%= params[:line] %>
    <% @cars.each do |car| %>
      <h4><%= link_to car.line, car_path(car) %>
          <% end %></h4>
  </div>